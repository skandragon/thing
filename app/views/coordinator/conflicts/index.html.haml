- title "Scheduling Conflicts"

- if @conflicts.size == 0
  %h2 No conflicts found.
- else
  %table.table.table-striped
    %thead
      %tr
        %th Type
        %th Class Name
        %th Instance Time and Location
        %th Duration
        %th Instructor
        %th Track
    %tbody
      - for conflict in @conflicts
        - type, instances = conflict
        %tr
          %td
            - type.each do |specific_type|
              %span.label.label-important= specific_type
          %td
            = instances[0].instructable.name
            %br
            = instances[1].instructable.name
          %td
            = instances[0].formatted_location_and_time
            %br
            = instances[1].formatted_location_and_time
          %td
            = instances[0].instructable.duration
            %br
            = instances[1].instructable.duration
          %td
            = instances[0].instructable.user.titled_sca_name
            %br
            = instances[1].instructable.user.titled_sca_name
          %td
            = instances[0].instructable.track
            %br
            = instances[1].instructable.track

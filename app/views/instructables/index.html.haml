- title "Classes"

%p.alert.alert-block.alert-info
  You have requested #{@target_user.instructables_session_count} of your
  allowed #{@target_user.instructor_profile.class_limit} classes.
  Only classes being taught in Pennsic University provided spaces count
  against your total.

%table.table
  %thead
    %tr
      %th.delete_clicky Delete
      %th Title
      %th Repeat
      %th Culture and Topic
      %th Status
      %th Scheduled Date
  %tbody
    = render :partial => @instructables

- if @target_user.instructor_profile.class_limit <= @target_user.instructables_session_count
  %p.alert.alert-warning
    You are over your class session limit.
    While additional classes may be added, you should contact
    Pennsic University staff to discuss your desire to teach more sessions.
.add-button= link_to 'Request a class', new_user_instructable_path(@target_user), :class => "btn btn-primary"

= page_navigation_links @instructables
